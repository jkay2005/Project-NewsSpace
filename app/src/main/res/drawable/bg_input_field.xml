<?xml version="1.0" encoding="utf-8"?>
<!-- Đây là file res/drawable/bg_input_field.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền bên trong của hình -->
    <solid android:color="@color/white" />

    <!-- Định nghĩa các góc bo tròn -->
    <!-- Chúng ta tham chiếu đến dimens.xml để dễ dàng thay đổi sau này -->
    <corners android:radius="@dimen/corner_radius_input" />

    <!-- Định nghĩa đường viền -->
    <stroke
        android:width="1dp"
        android:color="@color/input_field_border" />

</shape>