<?xml version="1.0" encoding="utf-8"?>
<!-- 1. Sử dụng CoordinatorLayout làm gốc. Đây là điều BẮT BUỘC
        để BottomAppBar và FloatingActionButton có thể tương tác với nhau. -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Vùng hiển thị nội dung chính (Fragment) không thay đổi -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment_main"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/main_nav_graph" />

    <!-- 2. Sử dụng com.google.android.material.bottomappbar.BottomAppBar.
        Đây là component đặc biệt tạo ra hiệu ứng "cái nôi". -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:fabCradleMargin="10dp"
        app:fabCradleRoundedCornerRadius="20dp"
        app:hideOnScroll="true"> <!-- Tùy chọn: Tự động ẩn khi cuộn -->

        <!-- 3. BottomNavigationView bây giờ được đặt BÊN TRONG BottomAppBar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigationView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginEnd="16dp"
            android:background="@android:color/transparent"
            app:elevation="0dp"
            app:menu="@menu/bottom_nav_menu" />

    </com.google.android.material.bottomappbar.BottomAppBar>

    <!-- 4. FloatingActionButton (FAB) được "neo" vào BottomAppBar -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="addblog"
        android:src="@drawable/ic_nav_add"
        app:backgroundTint="@color/primary_blue"
        app:layout_anchor="@+id/bottomAppBar"
        app:layout_anchorGravity="top|center"
        app:tint="@color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>