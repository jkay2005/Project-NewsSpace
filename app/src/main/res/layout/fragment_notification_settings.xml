<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".NotificationSettingsFragment"><com.google.android.material.appbar.MaterialToolbar
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    app:navigationIcon="@drawable/ic_arrow_back"
    app:title="Thông báo"
    app:titleCentered="true" />

    <!-- Phần Tin tức -->
    <TextView
        style="@style/TextAppearance.MaterialComponents.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="16dp"
        android:text="Tin tức" />

    <include
        android:id="@+id/newsNotificationsLayout"
        layout="@layout/item_setting_switch" />

    <include
        android:id="@+id/blogNotificationsLayout"
        layout="@layout/item_setting_switch" />

    <include
        android:id="@+id/categoryNotificationsLayout"
        layout="@layout/item_setting" />

    <!-- Phần Tiện ích -->
    <TextView
        style="@style/TextAppearance.MaterialComponents.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="24dp"
        android:text="Tiện ích" />

    <include
        android:id="@+id/weatherNotificationsLayout"
        layout="@layout/item_setting_switch" /></LinearLayout>