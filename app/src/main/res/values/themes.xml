<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsSpace" parent="Theme.Material3.DayNight.NoActionBar">
        <!--
          Sử dụng DayNight.NoActionBar là tốt nhất.
          - DayNight: Tự động hỗ trợ cả Chế độ Sáng và Tối.
          - NoActionBar: Vì chúng ta thường tự tạo Toolbar/Header riêng.
      -->

        <!-- Tùy chỉnh các màu chính của bạn ở đây -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- ... các màu khác ... -->
    </style>

    <style name="Theme.NewsSpace" parent="Base.Theme.NewsSpace" />

    <style name="Widget.App.SectionHeader.TextView" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/text_size_headline</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.App.Button.Primary" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:backgroundTint">@color/primary_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_height">56dp</item> <!-- Ước tính chiều cao -->
        <item name="cornerRadius">@dimen/corner_radius_button</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Widget.App.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_input_field</item> <!-- Cần tạo file drawable -->
        <item name="android:paddingStart">@dimen/spacing_medium</item>
        <item name="android:paddingEnd">@dimen/spacing_medium</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:textColorHint">@color/text_hint</item>
    </style>

    <!-- Style cho các tab chuyên mục có thể click -->
    <style name="Widget.App.CategoryChip" parent="Widget.MaterialComponents.Chip.Action">
        <item name="chipBackgroundColor">@color/chip_background</item>
        <item name="android:textColor">@color/text_primary</item>
        <!-- Thêm các thuộc tính khác nếu cần -->
    </style>

<!--    &lt;!&ndash; Style cho các tiêu đề tin tức &ndash;&gt;-->
<!--    <style name="TextAppearance.App.NewsTitle.Large" parent="TextAppearance.MaterialComponents.Headline6">-->
<!--        <item name="android:textSize">@dimen/text_size_news_title_large</item>-->
<!--        <item name="android:textStyle">bold</item>-->
<!--        <item name="android:lineSpacingMultiplier">1.1</item>-->
<!--    </style>-->

    <!-- Style cho tiêu đề tin tức LỚN (trong thẻ featured) -->
    <style name="TextAppearance.App.NewsTitle.Large" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">@dimen/text_size_news_title_large</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:lineSpacingMultiplier">1.1</item>
    </style>

    <!-- Style cho tiêu đề tin tức NHỎ (trong thẻ standard) -->
    <style name="TextAppearance.App.NewsTitle.Small" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textSize">@dimen/text_size_news_title_small</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:lineSpacingMultiplier">1.2</item>
    </style>
</resources>